#include <iostream>
#include <iomanip>
#include<stdlib.h>
#define ARRAY_Size 100
bool program = true;
using namespace std;


struct Course
{
    int courseId;
    int creditHours;
    string courseName;
    };

// The list that sotore all courses.
Course courses[ARRAY_Size];
int number_of_courses=0;


struct Student
{
    string firstName;
    string fatherName;
    string familyName;
    string fullName;
    int Student_id;
    
    void createFullName() {
        fullName = firstName + " " + fatherName + " " + familyName;
    }
};
// The list that sotore all student.
Student listOfStudent[ARRAY_Size];
int number_of_student=0;


//The function to add course.
void addCourse()
{
    if (number_of_courses < ARRAY_Size)
    {
        Course newCourse;

        cout << "Enter Course ID: ";
        cin >> newCourse.courseId;

        cout << "Enter Course Name: ";
        cin >> newCourse.courseName;

        cout << "Enter Course hours: ";
        cin >> newCourse.creditHours;

        courses[number_of_courses] = newCourse;
        number_of_courses++;

        cout << "Course added successfully!\n";
    }
    else
    {
        cout << "Cannot add more courses, limit reached.\n";
    }
    int input;
    cout << "Press 0 to exit from applicatin or any number to get back to main page: ";
    cin >> input;
    if (input ==0)
        program= false;
}


//The function to update course
void updateCourse(int courseId)
{
    bool found = false;

    for (int i = 0; i < number_of_courses; ++i)
    {
        if (courses[i].courseId == courseId)
        {
            cout << "Updating course with ID " << courseId << endl;

            cout << "Enter new Course Name: ";
            cin >> courses[i].courseName;

            cout << "Enter new Course hours:  ";
            cin >> courses[i].creditHours;

            found = true;
            cout << "Course updated successfully!\n";
            break;
        }
    }

    if (!found)
    {
        cout << "Course with ID " << courseId << " not found.\n";
    }
    int input;
    cout << "Press 0 to exit from applicatin or any number to get back to main page: ";
    cin >> input;
    if (input ==0)
        program= false;
    
}

//The function to delete course
void deleteCourse(int courseId)
{
    bool found = false;

    for (int i = 0; i < number_of_courses; ++i)
    {
        if (courses[i].courseId == courseId)
        {
            for (int j = i; j < number_of_courses - 1; ++j)
            {
                courses[j] = courses[j + 1];
            }

            number_of_courses--;
            found = true;

            cout << "Course with ID " << courseId << " deleted successfully!\n";
            break;
        }
    }

    if (!found)
    {
        cout << "Course with ID " << courseId << " not found.\n";
    }
    cout << "Press 0 to exit from applicatin or any number to get back to main page: ";
    int input;
    cin >> input;
    if (input ==0)
        program= false;
}




//The function to add student.
void addStudent()
{
    if (number_of_student < ARRAY_Size) {
        Student newStudent;
        
        cout << "Enter first name: ";
        cin >> newStudent.firstName;
        cout << "\nEnter father name: ";
        cin >> newStudent.fatherName;
        cout << "\nEnter family name: ";
        cin >> newStudent.familyName;
        cout << "\nEnter Student ID: ";
        cin >> newStudent.Student_id;
        
        newStudent.createFullName();
        
        listOfStudent[number_of_student] = newStudent;
        cout <<"Student: \"" << listOfStudent[number_of_student].fullName << "\" has been added successfully.";
        number_of_student++;
    }
    else {
        cout << "Cannot add more students, limit reached.\n";
    }
    int input;
    cout << "\nPress 0 to exit from applicatin or any number to get back to main page: ";
    cin >> input;
    if (input ==0)
        program= false;
}



// The function to update student.
void updateStudent(int studentId)
{
    bool found = false;
    for (int i = 0; i < number_of_student; ++i) {
        if (listOfStudent[i].Student_id == studentId) {
            cout << "Updating student with ID " << studentId << endl;
            cout << "old First name: " << listOfStudent[i].firstName;
            cout << ".\nEnter new first name: ";
            cin >> listOfStudent[i].firstName;
            cout << "old Father name : " << listOfStudent[i].fatherName;
            cout << ".\nEnter new father name: ";
            cin >> listOfStudent[i].fatherName;
            cout << "old Family name: " << listOfStudent[i].familyName;
            cout << ".\nEnter new family name: ";
            cin >> listOfStudent[i].familyName;
            listOfStudent[i].createFullName();
            found = true;
            cout <<"Student \"" << listOfStudent[i].fullName << "\" has been updated successfully.";
            
            break;
        }
    }
    if (!found) {
        cout << "Student with ID " << studentId << " not found." << endl;
    }
    int input;
    cout << "Press 0 to exit from applicatin or any number to get back to main page: ";
    cin >> input;
    if (input ==0)
        program= false;
}



// The function to delete a student.
void deleteStudent( int studentId)
{
    bool found = false;
    for (int i = 0; i < number_of_student; ++i)
    {
        if (listOfStudent[i].Student_id == studentId)
        {
            for (int j = i; j < number_of_student - 1; ++j)
            {
                listOfStudent[j] = listOfStudent[j + 1];
            }
            number_of_student--;
            found = true;
            cout << "Student with ID " << studentId << " deleted successfully!" << endl;
            break;
        }
    }
    if (!found) {
        cout << "Student with ID " << studentId << " not found." << endl;
        
    }
    int input;
    cout << "Press 0 to exit from applicatin or any number to get back to main page: ";
    cin >> input;
    if (input ==0)
        program= false;
}

void StudentsReport(int studentID){}



void greeting(){
    
    cout << string(50,'#');
    cout << "\n\n"
         << setw(40)
         <<"Student Registration System.\n\n";
    
    for(int i = 0;i<50;i++)
        cout << '#';
         
    cout <<"\n\n"
         <<"1.Add new Student/course.\n"
         <<"2.Assign/Remove courses for the students.\n"
         <<"3.Search and View student/course details.\n"
         <<"4.Update/Delete student record.\n"
         <<"5.Update/Delete course record.\n"
         <<"6.Students/courses report.\n"
         <<"7.Quit form the program.\n";
         
    }
    
    
void FirstOption(){
    
    int option;
    system("cls");
    cout << string(45,'#')
         << "\n\n"
         << setw(40)
         <<"Adding new Student/course.\n\n"
         << string(45,'#')<<endl
         << "\n1.Add new student.\n"
         << "2.Add new course.\n"
         << "3.Go back.\n";
    do{
        cout <<"Option:";
        cin>>option;
        switch(option){
        case 1:
            // The adding student function();
            addStudent();  //Done.
            break;
        case 2:
            // The adding Course function();
            addCourse(); //Done.
            break;
        case 3:
            break;
        default:
            cout << "Invalid input"<<endl;
    }
    }while(option>3&&1<option);
    
    
    
}
    
void SecondOption(){
    
    int option;
    system("cls");
    cout << string(45,'#')
         << "\n\n"
         << setw(40)
         <<"Assign/Removing courses for a student.\n\n"
         << string(45,'#')<<endl
         << "\n1.Assign course to a student.\n"
         << "2.Remove course from a student.\n"
         << "3.Go back.\n";
    do{
        cout <<"Option:";
        cin>>option;
        switch(option){
            case 1:
                // The assigning cousrse to student function();
                break;
            case 2:
                // The Remove course from a student function();
                break;
            case 3:
                break;
            default:
                cout << "Invalid input"<<endl;
        }

    }while(option>3&&1<option);
    
        
}

void ThirdOption(){
    int option;
    system("cls");
    cout << string(45,'#')
         << "\n\n"
         << setw(40)
         <<"Searching and View student/course details.\n\n"
         << string(45,'#')<<endl
         << "\n1.Searching for a student.\n"
         << "2.Searching for a course.\n"
         << "3.Go back.\n";
    do{
        cout <<"Option:";
        cin>>option;
        switch(option){
            case 1:
                // The Searching for a student function();
                break;
            case 2:
                // The Searching for a course function();
                break;
            case 3:
                break;
            default:
                cout << "Invalid input"<<endl;
        }

    }while(option>3&&1<option);
    
        
}

void FourthOption(){
    int option;
    int studentID;
    system("cls");
    cout << string(45,'#')
         << "\n\n"
         << setw(40)
         <<"Updating and Deleting student record.\n\n"
         << string(45,'#')<<endl
         << "\n1.Delete a student record.\n"
         << "2.Update a student record.\n"
         << "3.Go back.\n";
    do{
        cout <<"Option:";
        cin>>option;
        switch(option){
            case 1:
                // Delete a student record function();
                cout << "Enter a student ID: "; // Done.
                cin >> studentID;
                deleteStudent(studentID);
                break;
            case 2:
                // Update a student record function();
                cout << "Enter a student ID: "; // Done.
                cin >> studentID;
                updateStudent( studentID);
                break;
            case 3:
                break;
            default:
                cout << "Invalid input"<<endl;
        }

    }while(option>3&&1<option);
    
        
}
    
void FifthOption(){
    int courseId;
    int option;
    system("cls");
    cout << string(45,'#')
         << "\n\n"
         << setw(40)
         <<"Updating and Deleting course record.\n\n"
         << string(45,'#')<<endl
         << "\n1.Delete a course record.\n"
         << "2.Update a course record.\n"
         << "3.Go back.\n";
    do{
        cout <<"Option:";
        cin>>option;
        switch(option){
            case 1:
                // Delete a course record function();
                cout << "Enter couse ID: "; //Done.
                cin >> courseId;
                deleteCourse(courseId);
                break;
            case 2:
                // Update a course record function();
                cout << "Enter couse ID: "; //Done.
                cin >> courseId;
                updateCourse(courseId);
                break;
            case 3:
                break;
            default:
                cout << "Invalid input"<<endl;
        }
    }while(option>3&&1<option);
    
   
    
}

void SixthOption(){
    
    int option;
    system("cls");
    cout << string(45,'#')
         << "\n\n"
         << setw(40)
         <<"Students and courses report.\n\n"
         << string(45,'#')<<endl
         << "\n1.Students Report.\n"
         << "2.Couses Report.\n"
         << "3.Go back.\n";
    do{
        cout <<"Option:";
        cin>>option;
        switch(option){
            case 1:
                // Students Report function();
                break;
            case 2:
                // Couses Report function();
                break;
            case 3:
                break;
            default:
                cout << "Invalid input"<<endl;
        }
    }while(option>3&&1<option);
    
    
}


int main(){
    int option;
    while(program){
        system("cls");
        greeting();
        
        do{
            cout <<"Option:";
            cin  >> option;
            switch(option){
                    
                case 1: FirstOption(); break;
                    
                case 2: SecondOption(); break;
                    
                case 3: ThirdOption(); break;
                    
                case 4: FourthOption(); break;
                    
                case 5: FifthOption(); break;
                    
                case 6: SixthOption(); break;
                    
                case 7: program = false; break;
                    
                default: cout << "Invalid input"<<endl; }
            
        }
        while(option>7 || option<1);
    }

    return 0;
}






